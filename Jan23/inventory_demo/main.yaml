---
- name: test inventory
  hosts: all
  tasks:
    - name: print my variable
      ansible.builtin.debug:
        var: name
    - name: print my other variable
      ansible.builtin.debug:
        var: group